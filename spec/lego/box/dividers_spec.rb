require "spec_helper"

RSpec.describe Lego::Box::Dividers do
  it "has a version number" do
    expect(Lego::Box::Dividers::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
